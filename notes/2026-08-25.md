# Scratchpad — 2026-08-25

- Looked into dbt incremental materializations again; need to test `on_schema_change` with a real column rename.
- Airflow DAG: the retry backoff logic should probably live in `default_args`, not per-task.
- Reminder: refactor the Paris apartment price scraper to use `httpx` instead of `requests`.
- Possible idea: small CLI to convert CSV exports into Markdown tables for these notes.
- Check if PostGIS is worth adding to the local dev stack for geodata experiments.
